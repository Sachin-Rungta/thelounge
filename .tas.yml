---
framework: mocha
postMerge:
  pattern:
    - "./test/tests/*.js"
preMerge:
  pattern:
    - "./test/tests/*.js"
preRun:
  command:
    - yarn install
    - yarn run build
postRun:
  command:
    - yarn --version
    - yarn --version
version: 1.0
